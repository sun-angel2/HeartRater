<Window x:Class="PulseLink.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="心率连接" 
        Height="320" Width="260"
        Background="{StaticResource GlassBg}" 
        WindowStyle="None" 
        AllowsTransparency="True"
        Topmost="True" 
        ResizeMode="NoResize" 
        MouseLeftButtonDown="Window_MouseDown">

    <Border BorderBrush="{StaticResource NeonGreen}" BorderThickness="1" CornerRadius="5" Margin="5">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <DockPanel LastChildFill="False">
                <TextBlock Text="脉冲 // 连接" Foreground="White" FontWeight="Bold" FontFamily="Consolas" VerticalAlignment="Center"/>
                <CheckBox Content="幽灵模式" Foreground="Gray" FontSize="10" 
                          VerticalAlignment="Center" DockPanel.Dock="Right"
                          Checked="ToggleClickThrough" Unchecked="ToggleClickThrough"
                          ToolTip="启用鼠标穿透，方便游戏时使用"/>
            </DockPanel>

            <!-- Main Content -->
            <StackPanel Grid.Row="1" VerticalAlignment="Center">
                <!-- BPM Display -->
                <TextBlock Text="{Binding Bpm}" 
                           Foreground="{StaticResource NeonGreen}" 
                           FontSize="70" 
                           FontWeight="Bold" 
                           HorizontalAlignment="Center"
                           FontFamily="Arial">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="#00FF41" BlurRadius="20" ShadowDepth="0"/>
                    </TextBlock.Effect>
                </TextBlock>
                
                <TextBlock Text="次/分钟" Foreground="White" HorizontalAlignment="Center" FontSize="12" Opacity="0.5" Margin="0,-10,0,10"/>
                
                <TextBlock Text="{Binding Status}" Foreground="Gray" HorizontalAlignment="Center" FontSize="10" FontFamily="Consolas" Margin="0,0,0,5"/>

                <!-- Device List -->
                <ListBox ItemsSource="{Binding Devices}" 
                         Height="70" 
                         Background="Transparent" 
                         BorderThickness="0" 
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding Name}" 
                                    Command="{Binding DataContext.ConnectCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                                    CommandParameter="{Binding Id}" 
                                    FontSize="10" 
                                    Width="180"
                                    Margin="0,2"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>

            <!-- Footer Controls -->
            <StackPanel Grid.Row="2">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Button Content="扫描" Command="{Binding ScanCommand}" Margin="0,0,2,0"/>
                    <Button Grid.Column="1" Content="直播" Command="{Binding ToggleStreamCommand}" Margin="2,0,0,0"/>
                </Grid>
                
                <TextBox Text="{Binding StreamUrl}" 
                         Background="Transparent" 
                         Foreground="#555" 
                         BorderThickness="0" 
                         FontSize="9" 
                         Margin="0,5,0,0" 
                         IsReadOnly="True"
                         FontFamily="Consolas"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>