<Window x:Class="PulseLink.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=viewModels:MainViewModel, IsDesignTimeCreatable=True}"
        Title="{Binding Source={StaticResource LocalizedStrings}, Path=Strings.App_Title}"
        Height="320" Width="260"
        Background="{StaticResource GlassBg}" 
        WindowStyle="None" 
        AllowsTransparency="True"
        Topmost="True" 
        ResizeMode="NoResize" 
        MouseLeftButtonDown="Window_MouseDown">

    <Window.Resources>
        <Style x:Key="LangButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Padding" Value="2"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Width" Value="20"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="2"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#2200FF41"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border BorderBrush="{StaticResource NeonGreen}" BorderThickness="1" CornerRadius="5" Margin="5">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <DockPanel LastChildFill="False">
                <TextBlock Text="{Binding Source={StaticResource LocalizedStrings}, Path=Strings.Label_Header}" Foreground="White" FontWeight="Bold" FontFamily="Consolas" VerticalAlignment="Center"/>
                
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" VerticalAlignment="Center">
                    <Button Content="中" Style="{StaticResource LangButton}" Command="{Binding SwitchLanguageCommand}" CommandParameter="zh-CN"/>
                    <Button Content="EN" Style="{StaticResource LangButton}" Command="{Binding SwitchLanguageCommand}" CommandParameter="en-US"/>
                    <CheckBox Content="{Binding Source={StaticResource LocalizedStrings}, Path=Strings.Checkbox_GhostMode}" Foreground="Gray" FontSize="10" 
                              VerticalAlignment="Center" Margin="5,0,0,0"
                              Checked="ToggleClickThrough" Unchecked="ToggleClickThrough"
                              ToolTip="{Binding Source={StaticResource LocalizedStrings}, Path=Strings.Tooltip_GhostMode}"/>
                </StackPanel>
            </DockPanel>

            <!-- Main Content -->
            <StackPanel Grid.Row="1" VerticalAlignment="Center">
                <!-- BPM Display -->
                <TextBlock Text="{Binding Bpm}" 
                           Foreground="{StaticResource NeonGreen}" 
                           FontSize="70" 
                           FontWeight="Bold" 
                           HorizontalAlignment="Center"
                           FontFamily="Arial">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="#00FF41" BlurRadius="20" ShadowDepth="0"/>
                    </TextBlock.Effect>
                </TextBlock>
                
                <TextBlock Text="{Binding Source={StaticResource LocalizedStrings}, Path=Strings.Label_Bpm}" Foreground="White" HorizontalAlignment="Center" FontSize="12" Opacity="0.5" Margin="0,-10,0,10"/>
                
                <TextBlock Text="{Binding Status}" Foreground="Gray" HorizontalAlignment="Center" FontSize="10" FontFamily="Consolas" Margin="0,0,0,5" TextWrapping="Wrap"/>

                <!-- Device List -->
                <ListBox ItemsSource="{Binding Devices}" 
                         Height="70" 
                         Background="Transparent" 
                         BorderThickness="0" 
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding Name}" 
                                    Command="{Binding DataContext.ConnectCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                                    CommandParameter="{Binding Id}" 
                                    FontSize="10" 
                                    Width="180"
                                    Margin="0,2"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>

            <!-- Footer Controls -->
            <StackPanel Grid.Row="2">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Button Content="{Binding Source={StaticResource LocalizedStrings}, Path=Strings.Button_Scan}" Command="{Binding ScanCommand}" Margin="0,0,2,0"/>
                    <Button Grid.Column="1" Content="{Binding Source={StaticResource LocalizedStrings}, Path=Strings.Button_Live}" Command="{Binding ToggleStreamCommand}" Margin="2,0,0,0"/>
                </Grid>
                
                <TextBox Text="{Binding StreamUrl}" 
                         Background="Transparent" 
                         Foreground="#555" 
                         BorderThickness="0" 
                         FontSize="9" 
                         Margin="0,5,0,0" 
                         IsReadOnly="True"
                         FontFamily="Consolas"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>